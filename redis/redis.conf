# Redis configuration for persistence
appendonly yes
appendfsync everysec

save 900 1
save 300 10
save 60 10000

# Don't stop writes if background save fails (dev environment)
stop-writes-on-bgsave-error no

maxmemory 256mb
maxmemory-policy allkeys-lru

# Disable protected mode for Docker networking
protected-mode no
